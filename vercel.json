{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install && cd dashboard && npm install",
  "buildCommand": "cd dashboard && npm run build",
  "outputDirectory": "dashboard/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://YOUR_BACKEND_URL/api/$1"
    },
    {
      "source": "/videos/(.*)",
      "destination": "https://YOUR_BACKEND_URL/videos/$1"
    },
    {
      "source": "/thumbnails/(.*)",
      "destination": "https://YOUR_BACKEND_URL/thumbnails/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Cross-Origin-Opener-Policy",
          "value": "same-origin"
        },
        {
          "key": "Cross-Origin-Embedder-Policy",
          "value": "credentialless"
        }
      ]
    }
  ]
}
